syntax = "proto3";

package SpellChecker;

service SpellChecker {
    rpc check (Request) returns (Reply) {}
}

message Request {
    string sentence = 1;
}

message Reply {
    string sentence = 1;
}
